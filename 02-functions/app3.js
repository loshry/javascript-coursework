// psuedocode -- organizing thoughts 
// user clicks number button
    //  user continues to click numbers
    // each number is added to the FIRST NUMBER
// user clicks Operator button
    // Operator is stored in a variable
// user clicks more number buttons
    // create Second Number
// user clicks equal button
    // Calculate function uses FIRST NUMBER, SECOND NUMBER and OPERATOR to come up with result
// RESULT is shown on the screen

// Calculate Functions
    // ADD
    // SUBTRACT
    // DIVIDE
    // MULTIPLY
    // Can handle >2 numbers
    // PERCENTAGE
    // SQUARE ROOT

    const firstNumber = prompt("First number: ");
    const Operator = prompt("Operator: ");
    const secondNumber = prompt("secondNumber: ");